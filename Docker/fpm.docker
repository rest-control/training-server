FROM php:7.1-fpm

RUN apt-get update && apt-get install -y \
      libmcrypt-dev \
      zlib1g-dev \
      libssl-dev \
      libpq-dev \
      p7zip-full \
      libzip-dev \
      libmcrypt-dev \
      mysql-client \
    && docker-php-ext-install mcrypt mbstring pdo_mysql zip

WORKDIR /app